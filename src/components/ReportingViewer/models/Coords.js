/**
 * Table coordinates(row and column)
 *
 * @class
 * @constructor
 * @param {number} row - table row
 * @param {number} column - table column
 */
class Coords {
  constructor(row, column) {
    this.row = row
    this.column = column
  }
}

export default Coords
